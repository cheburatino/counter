docType = "Counterparty"
tableComment = "контрагент"

tableName ="counterparty"

fields = [
	{name="id",			type="serial"},
	{name="title",					type="char",	size=150, 	ext="not null",	 comment="название"},
	{name="search_text",			type="text",	comment="колонка для поиска"},
	{name="options",				type="jsonb",	comment="разные дополнительные параметры"},
	{name="created_at",				type="timestamp",	ext="with time zone"},
	{name="updated_at",				type="timestamp",	ext="with time zone"},
	{name="deleted",				type="bool",	ext="not null default false"}
]

fkConstraints = [
	{name="counterparty_title_already_exist", ext="UNIQUE (title)"}
]

triggers = [
	{name="counterparty_created", when="before insert or update", ref="for each row", funcName="builtin_fld_update"},
	{name="counterparty_trigger_before", when="before insert or update", ref="for each row", funcName="counterparty_trigger_before"},
	{name="counterparty_trigger_after", when="after insert or update", ref="for each row", funcName="counterparty_trigger_after"}
]



methods = [
	"counterparty_list",
	"counterparty_update",
	"counterparty_get_by_id",
	"counterparty_trigger_before",
	"counterparty_trigger_after"
]

alterScripts = [
	"alter table counterparty add column if not exists title CHARACTER VARYING(150);",
	"alter table counterparty add column if not exists search_text text;"
]