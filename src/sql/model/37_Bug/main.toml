docType = "Bug"
tableComment = "баг"

tableName ="bug"

fields = [
	{name="id",			type="serial"},
	{name="title",					type="char",	size=150, 	ext="not null",	 comment="название"},
	{name="state_id",					type="int",	 comment="статус"},
	{name="description",					type="text",	 comment="описание"},
	{name="functional_requirement_id",					type="int",	 comment="функциональное требование"},
	{name="system_id",					type="int",	 comment="система"},
	{name="search_text",			type="text",	comment="колонка для поиска"},
	{name="options",				type="jsonb",	comment="разные дополнительные параметры"},
	{name="created_at",				type="timestamp",	ext="with time zone"},
	{name="updated_at",				type="timestamp",	ext="with time zone"},
	{name="deleted",				type="bool",	ext="not null default false"}
]

fkConstraints = [
	{name="bug_title_already_exist", ext="UNIQUE (title)"},
	{fld="state_id", ref="ctlg_bug_state", fk="id"},
	{fld="functional_requirement_id", ref="functional_requirement", fk="id"},
	{fld="system_id", ref="system", fk="id"}
]

triggers = [
	{name="bug_created", when="before insert or update", ref="for each row", funcName="builtin_fld_update"},
	{name="bug_trigger_before", when="before insert or update", ref="for each row", funcName="bug_trigger_before"},
	{name="bug_trigger_after", when="after insert or update", ref="for each row", funcName="bug_trigger_after"}
]



methods = [
	"bug_update",
	"bug_get_by_id",
	"bug_list",
	"bug_trigger_before",
	"bug_trigger_after"
]

alterScripts = [
	"alter table bug add column if not exists title CHARACTER VARYING(150);",
	"alter table bug add column if not exists state_id int;",
	"alter table bug add column if not exists description text;",
	"alter table bug add column if not exists functional_requirement_id int;",
	"alter table bug add column if not exists system_id int;",
	"alter table bug add column if not exists search_text text;"
]